services:
  homelab-installer:
    build: .
    container_name: homelab-installer
    environment:
      ANSIBLE_HOST_KEY_CHECKING: "False"
      INSTALL_FORCE: "${INSTALL_FORCE:-false}"
    volumes:
      - ./secrets:/run/homelab-secrets:ro
