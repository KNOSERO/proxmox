## Agent skills

### Issue tracker

GitHub Issues in `KNOSERO/proxmox`. See `docs/agents/issue-tracker.md`.

### Triage labels

Workflow uses Jira-style labels: `backlog`, `to-do`, `in-progress`, `in-review`, `done`, `blocked`, and `wont-do`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repository. See `docs/agents/domain.md`.
